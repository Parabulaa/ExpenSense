name: Publish preview update

jobs:
  publish_update:
    name: Publish update
    type: update
    environment: preview
    params:
      channel: preview
      platform: all
      message: Free OCR.space receipt reading
