name: Publish preview update

jobs:
  publish_update:
    name: Publish update
    type: update
    environment: preview
    params:
      channel: preview
      platform: all
      message: Wallet transfers, receipt classification, period navigation
